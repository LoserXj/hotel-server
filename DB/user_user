create table user_user
(
    id       serial
        primary key,
    name     varchar,
    password varchar,
    tel      varchar,
    sex      varchar
);

alter table user_user
    owner to postgres;